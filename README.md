# GECS

Esta es la guía de instalación y ejecución del **Sistema **, compuesto por un **Backend** (servidor y base de datos) y un **Frontend** (interfaz de usuario).

---

## ⚙️ Requisitos Previos

Para ejecutar el sistema, es **imprescindible** tener instalado:

* **Node.js**: La plataforma de ejecución de JavaScript necesaria para correr los scripts de `npm`.

---

## 🛠️ Instalación del Proyecto

Sigue estos pasos para configurar el proyecto e instalar todas las dependencias.

### 1. Obtener el Código

### 2. Instalar el node:

```bash
cd gestor
npm install
```

### 3. En el terminal:

```bash
cd gestor/backend
npm run dev
```

### 3. En otro terminal:

```bash
cd gestor
npm start
```
